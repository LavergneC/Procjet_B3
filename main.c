#include "calculs.h"
#include <stdio.h>

int main(void){
    /*
    VariablesPID dxPID, dyPID, rotPID;
    unsigned char i,j;
    double k = 1.0;
    signed char vOut[4]={0x55,0x55,0x55,0x55};
    
    initPID(&dxPID, k,k,k);
    initPID(&dyPID, k,k,k);
    initPID(&rotPID,k,k,k);

    getVMoteurs(32767,32767,32*254,vOut);

    printf("*---------------------------*\n");
    printf("  Valeur sortie moteur :\n");
    for(i=0; i<4; i++)
        printf("    Moteur %d : %d\n", i+1, vOut[i]);
*/
  return 0;
}